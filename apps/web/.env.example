# Esempio di file .env per VITA
# Copia questo file in .env e sostituisci i valori con i tuoi

DATABASE_URL=postgresql://username:password@localhost:5432/vita_db
JWT_SECRET=supersecret

# Imposta DEMO_MODE=true per avviare VITA senza dipendenze esterne.
# Quando DEMO_MODE è true l'applicazione non proverà a connettersi a
# PostgreSQL né ad altre API e userà un file JSON locale per i dati di
# esempio. Lascialo vuoto o metti "false" per l'utilizzo normale.
DEMO_MODE=